spring:
  mongodb:
    # DynamicPropertySource substitui a URI inteira pelo contêiner de testes.
    # Sem esse suporte, falha em vez de acessar o banco local.
    uri: ${TEST_MONGODB_URI}

inovagab:
  security:
    jwt:
      secret: aW5vdmFnYWItdGVzdC1qd3Qtc2VjcmV0LWtleS0zMi1ieXRlcyE=
      issuer: inovagab-api-test
      expiration: PT1H
